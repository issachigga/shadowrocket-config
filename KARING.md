# Karing 原生分流教程（iOS 测试版）

[下载 karing-routing.json](https://raw.githubusercontent.com/issachigga/shadowrocket-config/main/karing-routing.json)

这是 **Karing 原生自定义分流文件**，不是完整 sing-box JSON，也不是机场节点订阅。使用普通模式，不启用「使用原始 sing-box 配置」。因此保留首页手动选择节点和原生订阅更新机制。

**分流文件不包含 DNS/VPN 全部设置，导入后必须完成下面的 DNS 操作，不能只导入文件就认定防泄漏完成。**不含私人订阅、真实节点、MITM 或 YouTube 模块。

## 一、先添加自己的订阅

从 [官方项目](https://github.com/KaringX/karing) 的下载入口安装最新版。备份现有设置；若曾启用原始配置，在「关于 → 使用原始 sing-box 配置」清除原始配置选择，恢复普通模式。

在「添加配置 → 添加配置链接」填自己的订阅，更新并启用。推荐使用服务商直接提供的兼容订阅，不通过不可信转换网站。导入时不要启用机场/ISP 分流规则；在「设置 → 分流 → 分流规则」开启禁用 ISP 分流规则，避免机场规则覆盖我们的选择。

## 二、导入原生分流

1. 下载本仓库 `karing-routing.json`，保存到文件 App。
2. 打开「设置 → 分流 → 分流规则 → 编辑/自定义分流组」，在菜单选「导入」，选择这个 JSON。
3. 检查导入预览，确认规则已启用后保存。此格式按照项目内置 preset 和历史导入实现编写；按钮位置可能随版本改变。若预览为空或报错，停止导入并反馈版本，不要用“添加订阅”代替。
4. 核对分流顺序：局域网、银行/百度在前；服务分组其次；国内域名、中国 IP 在后；**其他与未知目标必须最后**。已有规则可能保留原顺序或追加在前，导入不是清空全机规则，请检查并禁用冲突规则，不要盲目重复导入。
5. 外国服务和兜底的动作必须是 **当前选择**，不是「自动选择」，也不要单独绑定某个节点。Apple、小米、网易云音乐、国内网站、中国 IP 为「直连」。文件中的 `currentSelected` 就是官方「当前选择」动作。
6. 确保私有网络直连；不要额外启用与本文件重复/冲突的预设组。若某个国外域名被国内规则误收录，在国内组前增加「当前选择」例外，并核对 DNS。

## 三、必须手动设置 DNS

需要关闭「新手模式」以显示完整选项。不要点「自动设置 DNS 服务器」，它可能按延迟改成不同上游。

在「设置 → DNS」开启 **启用 DNS 分流规则**；「代理流量解析通道」选择 **代理**，不是直连。当前源码此选项包含代理/直连/FakeIP；旧版本若分别提供“代理流量通过代理解析 DNS”和“final 通过代理解析 DNS”，两项都要开启。找不到对应选项时先不要认为路径已相同。

在「DNS → 服务器」分别配置如下（只保留对应类别里的这些 DoH，不混入 Local、DHCP、UDP DNS）：

| 类别 | 上游 |
|---|---|
| DNS 服务器 / bootstrap | `https://223.5.5.5/dns-query` |
| 代理服务器（节点入口解析） | `https://223.5.5.5/dns-query`、`https://1.12.12.12/dns-query` |
| 直连流量 | `https://223.5.5.5/dns-query`、`https://1.12.12.12/dns-query` |
| 代理流量 | `https://1.1.1.1/dns-query`、`https://1.0.0.1/dns-query` |
| final（如果版本单独提供） | 与代理流量相同 |

国内 DoH 解析节点入口仍会让国内 DNS 服务商知道节点域名，这是与国外网站查询不同的边界。DoH 加密不等于匿名，DNS 服务商仍可看到查询。

不启用 ECS；首次版本不使用 FakeIP，方便核对实际 DoH 路径。IPv6 选择“优先 IPv4”/双栈模式，而不是“仅 IPv4”，保留 IPv6但不强制优先。允许 QUIC/UDP，不另外加禁用 UDP 443 规则。实际字段和选项以当前发布版本为准。

DNS 的 UI 设置需要落入运行时生成配置，不能只看勾选。连接后从「关于 → 文件内容查看」等版本提供的核心配置/日志入口核对：默认/代理 DNS 是国外 DoH，detour 指向当前节点；直连和代理入口使用国内 DoH，不存在国外查询转 Local/DHCP/国内备用的路径。分享核心配置之前先脱敏所有订阅、UUID、密码和接口 secret。

## 四、首页选节点

回首页手动选一条真实节点，不能选 DIRECT、自动选择或自动最快节点。使用分流/规则模式，重新连接 VPN。国外组的「当前选择」将跟随首页选择；没有另建自动选节点组。切换后已有长连接可能暂时保留，需要重连或重新打开目标 App，再检查新连接出口。

如果节点失败，不要让自动容灾转 DIRECT。若版本提供失效节点处理/自动切换功能，请核对实际行为；本仓库未实机确认这些故障路径。VPN 关闭后的系统直连不由分流文件阻止。

## 更新和测试

节点由 Karing 的私人订阅更新，规则组关联的是「当前选择」，不写死机场节点名。远程 SRS 规则由客户端自己的下载/缓存机制管理，需检查分流规则探测和下载日志；本文件无法指定每台手机准时每日刷新。修改公开分流模板后，下载并重新导入，保留私人订阅和 DNS 设置，不要公开整机备份。

原生导入只修改自定义分流，不自动修改 DNS、IPv6 或全部设置，也不保证消除已有规则冲突。远程规则错误或未下载时可能导致误分流；国外兜底放最后，规则不可用时未知目标应代理，但仍需验证实际故障行为。

内网 IP 直连；公共 DoH 不保证解析私有 NAS/PVE/OpenWrt 域名，可直接用内网 IP，或单独添加本地 DNS 规则。应用自带 DNS、VPN 排除、ECH/域名识别、规则误分类均是边界。

**未在 iOS Karing 实机运行，不能称为零泄漏成品。**已核对 19 个原生组的动作和 JSON 格式；16 个共用远程 SRS 下载成功，官方 sing-box 1.14.0 解码通过，但这不能替代 Karing 本身的导入/运行测试。加入订阅并完成 DNS 后，测试正常连接、切换首页节点、节点故障且 VPN 保持开启，分别检查 IPv4/IPv6、DNS 和连接日志。测试网站仅辅助，不能证明所有 App 不泄漏。

参考：[当前选择动作](https://karing.app/app-manual/diversion-rule)、[DNS](https://karing.app/app-manual/dns)、[DNS 服务器](https://karing.app/app-manual/dns-server)、[原生导入预设格式](https://github.com/KaringX/karing/blob/main/assets/datas/preset/cn.json)。
